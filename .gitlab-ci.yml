include:
  - remote: 'https://gitlab.com/cscs-ci/recipes/-/raw/master/templates/v2/.cscs.yml'

run:
    extends: .daint
    image: ubuntu:18.04
    script: echo "hello2"
    variables:
      PULL_IMAGE: 'YES'
